---
openclaw_binary: "/home/openclaw/.local/bin/openclaw"

# Pinned OpenClaw version. Set in site config to control upgrades.
# Use exact version (e.g. "2026.3.12") for reproducibility.
openclaw_version: "latest"
